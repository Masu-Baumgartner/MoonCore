@page "/"
@using Bogus
@using MoonCoreUI.Test.Data
@using MoonCoreUI.Components.Forms

<PageTitle>Index</PageTitle>

<AdvancedSmartDropdown T="WeatherForecast" Items="Forecasts" @bind-Value="Forecast" SearchProp="@(x => x.Summary)" DisplayFunc="@(x => x.Summary)" />

@code
{
    private List<WeatherForecast> Forecasts = new();
    private WeatherForecast Forecast;

    protected override void OnInitialized()
    {
        var faker = new Faker("de");
        
        for (int i = 0; i < 100; i++)
        {
            Forecasts.Add(new WeatherForecast()
            {
                Summary = faker.Address.City()
            });
        }
    }
}