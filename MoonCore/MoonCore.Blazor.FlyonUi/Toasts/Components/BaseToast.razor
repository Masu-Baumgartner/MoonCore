@code
{
    [Parameter] public ToastLauncher Launcher { get; set; }
    [Parameter] public ToastItem ToastItem { get; set; }

    /// <summary>
    /// Hides/removes this toast component
    /// </summary>
    public async Task HideAsync() => await Launcher.CloseAsync(ToastItem);
}
