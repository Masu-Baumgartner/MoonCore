@{
    var route = "/" + (Router.Route ?? "");
}

@if (route == Path)
{
    @ChildContent
}

@code
{
    [CascadingParameter]
    public MCBRouter Router { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
    
    [Parameter]
    public string Path { get; set; }
}