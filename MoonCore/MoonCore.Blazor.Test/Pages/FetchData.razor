@page "/fetchdata"
@using MoonCore.Blazor.Forms.Auto
@using MoonCore.Blazor.Test.Data

@inject ILogger<FetchData> Logger

<AutoFormGenerator Model="Form">
    <DefineCustomComponent T="bool">
        <div class="form-check">
            <input class="form-check-input" type="checkbox" @bind="context.Value">
            <label class="form-check-label" for="flexCheckDefault">
                Default checkbox
            </label>
        </div>
    </DefineCustomComponent>
</AutoFormGenerator>

<WButton OnClick="DoSmth">
    sdasjdklasjkdj
</WButton>

@code
{
    private TestForm Form = new();

    private async Task DoSmth(WButton _)
    {
        Logger.LogInformation(Form.Email);
        Logger.LogInformation(Form.Name);
        Logger.LogInformation(Form.A.ToString());
    }
}
